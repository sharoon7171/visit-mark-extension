VisitMark uses the scripting permission to insert and remove CSS in open tabs so visited links can be styled with your colors, and to run small injected scripts on ordinary web pages that collect link URLs from the page when needed and apply or clear history-based highlights—only on http and https documents the extension is allowed to access, and only as required to implement those features.
