VisitMark uses the tabs permission to discover open tabs and their URLs when applying or updating highlights after you change settings or when a tab finishes loading, to refresh the active tab’s styling when you switch to it, to drop internal bookkeeping when a tab closes, and so the popup can read the active tab’s hostname for per-site options—without collecting tab contents beyond what Chrome’s tab APIs already expose for these tasks.
